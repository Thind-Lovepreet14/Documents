// Create an object called person with name = John, age = 20. 
// Then, access the object to alert("John is 20").

const person = {
    name: 'John',
    age: 20
}

alert(`${person.name} is ${person.age}`);